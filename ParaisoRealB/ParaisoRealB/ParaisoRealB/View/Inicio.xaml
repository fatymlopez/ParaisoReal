<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ParaisoRealB.ViewModel"
             x:Class="ParaisoRealB.View.Inicio">

    <ContentPage.BindingContext>
        <vm:InicioVM></vm:InicioVM>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="25">
                <Label Text="Welcome to Paraiso Real Banquetes"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
                <Button Text="Inicio" Command="{Binding InicioCommand}"></Button>
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                    <Label Text="Siguenos en Facebook: @paraisorealbanquetes" HorizontalOptions="Center"></Label>
                    <Image Source="facebook.png" HeightRequest="25"></Image>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>